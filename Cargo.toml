[workspace]
members = [
    "kernel",
    "runner",
]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["Brane OS Team"]
license = "MIT"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
strip = true
